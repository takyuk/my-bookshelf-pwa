# ISBN / ASIN と Kindle 共有

## 入力と保存

- 形式は「紙」「電子書籍(Kindle)」「電子書籍(その他)」「その他」。新規入力の初期値は紙。
- カメラでISBNを正常に読み取ると紙へ切り替える。Kindle共有で新規入力を開始すると電子書籍(Kindle)へ切り替える。
- 紙とKindle以外の電子書籍はISBNを検証し13桁へ正規化。Kindleは10文字の英数字ASINを大文字へ正規化。どの形式も識別子の空欄を許可。
- その他は入力を保持（前後空白のみ除去）。識別できるISBN・ASINだけ重複判定する。
- `identifier: {type, value}` が識別子の保存元。`isbn` は既存のISBN検索・書影処理との互換用で、ASINを入れない。
- 旧電子書籍は電子書籍(その他)として読み込み、元の識別子を保持。読み込んだだけでは端末の保存データを書き換えない。
- 同じ値でもISBNとASINは別の識別子。ISBN-10とISBN-13は同一判定。編集中の本自身を重複から除外。
- 商品種別の自動照会は行わない。未確認のKindle ASINは保存前にユーザー確認を求め、紙本なら形式とISBNの修正を案内。確認済みASINを記録し同じ値で再確認しない。
- Kindleの商品ページリンクはASINから生成。購入・閲覧や外部送信はユーザーがリンクを開いたときのみ。購入日・刊行年月等は推測しない。
- Kindleの保管場所は新規ではKindle。既存の保管場所は保持。

## Kindle共有

manifestのGET share_targetでtitle / text / urlを受け取る。Android ChromeにインストールしたPWAをKindleの共有先に選択する。

対応はAmazon日本のread.amazon.co.jp/kp/kshare?asin=... と amazon.co.jp / www.amazon.co.jp のdp・gp/productリンク。短縮URL、他国ストア、複数冊同時は対象外。外部ページの取得はしない。

共有文の「\"タイトル\"（著者 著）」を解析し、タイトル・著者・ASINを編集画面へ入力。解析できない書誌項目は空欄。自動保存しない。規約未同意の場合は同意後に入力画面を開く。保存障害時は共有のお知らせから再試行できる。

共有内容を含むURLはService Workerのキャッシュに保存しない。入力画面に取り込んだ後、アドレスバーから共有パラメータを除去する。ただし共有時の通信URLはCloudflareなどのアクセス記録に残る可能性がある。

## 検索・書影・バックアップ

ASINによるNDL検索・Google書影取得は未対応で、その旨を表示する。ユーザーの撮影・画像選択は利用可能。紙版の書誌や書影をKindleへ自動転用しない。

ZIPには識別子の種類・値と確認済みASINを含む。旧JSON / ZIPも読み込める。新しいZIPは旧版アプリへ復元するとASINを扱えないため、この版以降で復元する。

## 検証

`node tests/regression.cjs` で移行・型別重複・共有解析・不正リンク等を検証。Chromeで共有URL→同意→入力→保存、確認の再表示抑止、ZIP復元、旧書籍編集を確認。

Android実機では公開後、Kindleの共有一覧に本アプリが現れること、共有内容の入力、Cloudflare Accessの再認証を挟んだ受け渡しを確認する。既存インストールへの共有先更新はブラウザ・OS側の反映に時間がかかる場合がある。再インストールを試す前に必ずZIPを書き出し、サイトデータを削除しない。
